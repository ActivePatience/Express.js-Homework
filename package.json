{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npm i express && node server.js",
    "dev": "npm i express && npm i inquirer -D && npm i nodemon -D && nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "inquirer": "^9.2.3",
    "nodemon": "^2.0.22"
  }
}
